/**
 * @file    cmsis_os.h
 * @brief   CMSIS-RTOS V1 호환 래퍼 — ThreadX CMSIS-RTOS V2 API로 리디렉션
 *
 * 기존 코드에서 #include "cmsis_os.h" 를 사용하는 파일들이
 * ThreadX 환경에서도 컴파일되도록 cmsis_os2.h를 포함합니다.
 */
#pragma once
#include "cmsis_os2.h"
